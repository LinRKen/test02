name: CI
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  workflow_dispatch:


jobs:
  test_01sh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: chmod 01sh script
        run: docker images

        


